# Error Journal — Kibah Asesores

## 1. View con columnas inventadas (M1)
- **Error:** El SQL de 001-foundation.sql usó columnas que no existen en base_kibah ("Nombre", "Tipo", "Precio")
- **Causa:** El prompt decía "genera el SQL del Master Doc" pero Claude Code no tiene acceso al Master Doc
- **Fix:** Se creó SQL corregido con columnas reales ("Nombre Desarrollador", "Precio por unidad", etc.)
- **Regla:** SIEMPRE verificar columnas reales leyendo la tabla en Supabase o el archivo SQL existente. NUNCA inventar nombres de columnas.

## 2. Carga de 2,600 propiedades sin paginación (M2)
- **Error:** La página se trabó y tumbó la computadora del usuario
- **Causa:** Se cargaron todas las propiedades de golpe sin respetar el límite de paginación
- **Fix:** Forzar LIMIT 20 por default, máximo 50 por request
- **Regla:** SIEMPRE paginar. NUNCA cargar más de 50 registros en una sola request. Default = 20.

## 3. Zod v4 API differences (M3)
- **Error:** Build failed because `z.enum()` second param uses `{ message: '...' }` not `{ errorMap: () => ({}) }`, and `z.number()` uses `{ error: '...' }` not `{ invalid_type_error: '...' }`. Also `ZodError.errors` doesn't exist — it's `ZodError.issues`.
- **Causa:** Project uses Zod v4 which has a simplified API compared to v3 docs.
- **Fix:** Changed to `z.enum(values, { message })`, `z.number({ error })`, and `err.issues`.
- **Regla:** This project uses Zod v4. Use `{ message }` for enums, `{ error }` for number/string, and `.issues` not `.errors` on ZodError.

## 4. Supabase JS client doesn't need SQL-escaped quotes in column names (M3)
- **Error:** `Could not find '% Comisión' column of 'base_kibah' in the schema cache` — insert/update failed.
- **Causa:** COLUMN_MAP had column names wrapped in escaped double quotes (`'"Nombre Kibah"'`). Supabase JS client handles quoting automatically — the object keys should be plain strings (`'Nombre Kibah'`).
- **Fix:** Removed all escaped double quotes from COLUMN_MAP values.
- **Regla:** When using Supabase JS `.insert()` / `.update()`, column names go as plain strings WITHOUT SQL quotes. The client handles quoting internally.

## 5. Campos numéricos no deben usar .int() si aceptan decimales (M3)
- **Error:** num_banos rechazaba 2.5 (medio baño) porque tenía `.int()` en el schema Zod.
- **Causa:** Se asumió que baños/recámaras/estacionamiento eran siempre enteros.
- **Fix:** Removido `.int()` de num_banos, num_recamaras, estacionamiento en el schema Zod.
- **Regla:** Solo usar `.int()` cuando el campo sea ESTRICTAMENTE entero. Baños/recámaras/estacionamiento pueden ser decimales (2.5 baños = medio baño).
## 6. Migración a proyecto Supabase de brika — propiedades ahora lee inventario_industrial
- **Error:** La app buscaba `propiedades_view`/`base_kibah` (esquema Kibah) en el proyecto Supabase nuevo de brika, que no tiene ese esquema; todo el módulo de propiedades daba 500.
- **Causa:** El código es un fork del CRM Kibah; el proyecto nuevo (`pmnxevxmaadtksoqifdo`) tiene los datos en `inventario_industrial` (naves/lotes industriales, mismo proyecto que usa el Laravel de brika).
- **Fix:** Se creó `src/lib/utils/inventario.ts` (mapa clave-frontend → columna real + mapper de filas). Las rutas GET `/api/properties` y `/api/properties/filter-options` y el DAL de lectura consultan `inventario_industrial` con el cliente admin (la tabla tiene RLS activado SIN políticas — la administra Laravel — así que el cliente de usuario vería 0 filas; las rutas verifican sesión antes de leer). Las escrituras (create/update/delete) siguen apuntando a `base_kibah` y NO funcionan aún.
- **Regla:** En este proyecto, toda lectura de inventario va vía `KEY_TO_COLUMN`/`mapInventarioRow` de `lib/utils/inventario.ts`. No crear tablas del esquema Kibah en la base de brika. Cambios de BD solo los aplica el usuario pegando SQL en el dashboard.

## 7. Alta/edición de propiedades migrada al esquema brika (inventario_industrial)
- **Error:** El modal "Agregar propiedad" mostraba los campos de Kibah (recámaras, baños, colonia CDMX...) y el guardado apuntaba a `base_kibah`, inexistente en la base de brika.
- **Causa:** Formulario y ruta de escritura heredados del fork sin adaptar.
- **Fix:** `PropertyForm` reescrito con los campos reales de `inventario_industrial` (parque, unidad, operación, superficies, precios venta/renta, características técnicas). Nuevo esquema Zod en `lib/validations/inventario.ts` (nombres 1:1 con columnas → sin remapeo). DAL/service/rutas escriben a `inventario_industrial` con id UUID (antes int). El constraint unique (parque, unidad, operacion) se traduce a error 409 amigable. El mapper de `lib/utils/inventario.ts` ahora expone también las columnas crudas para inicializar el formulario de edición (la columna `disponibilidad` viaja como `fecha_entrega` por colisión con el alias legacy).
- **Regla:** Cualquier campo nuevo de inventario se agrega en 3 lugares: schema Zod, mapper (`mapInventarioRow`), y el formulario. Verificado en vivo: POST 201, duplicado 409, PUT y DELETE 204 contra la tabla real (registro de prueba creado y eliminado).

## 8. Branding Kibah visible en PDF y UI (rebranding a BRIKA)
- **Error:** El PDF de propiedades (y la carta propuesta, login, sidebar, título de la app, mensajes) mostraban branding de Kibah: logo `kibah-logo-*.png`, texto `kibah.com.mx`, filename `Kibah-Propiedades-*.pdf`, labels "Nombre Kibah".
- **Causa:** Fork del CRM Kibah sin rebrandear; en `public/images/` solo existen logos de Kibah (no hay logo de brika).
- **Fix:** Reemplazo global de branding por BRIKA: prefijo interno `kibah-` → `brika-` (clases CSS, claves localStorage, rutas de logo), textos visibles → BRIKA, filename del PDF → `BRIKA-Propiedades-*.pdf`. Login/sidebar usan wordmark de texto "BRIKA"; los generadores de PDF/DOCX buscan `/images/brika-logo-dark.png` y si no existe dibujan "BRIKA" como texto (fallback). Carpeta Cloudinary de nuevas subidas: 'Kibah' → 'Brika'.
- **Regla:** NUNCA renombrar identificadores de datos al rebrandear: `nombre_kibah` (clave interna mapeada a `parque`), tablas/columnas `base_kibah`, `pagina_web_kibah`, `'Nombre Kibah'` (columna DB en carga-masiva/COLUMN_MAP), `kibah_category_*` (extendedProperties de Google Calendar en eventos existentes), y el preset de Cloudinary `kibah_unsigned` (config de la cuenta). Solo se rebrandea lo visible/UI. Cuando haya logo de brika, subir `brika-logo-dark.png` y `brika-logo-white.png` a `public/images/`.

## 9. Textos con charSpace desalineados en jsPDF (Ficha Técnica BRIKA)
- **Error:** Al generar la ficha técnica, los textos con letter-spacing (`charSpace`) alineados a la derecha se desbordaban del margen y salían cortados en el borde de la página (ej. "LOTE INDUSTRIAL · APASEO EL GRAN"); los centrados quedaban corridos. Además `fmtMoney` con `maximumFractionDigits: 0` mostraba "$2 / m²" en vez de "$2.20 / m²" para precios por m².
- **Causa:** jsPDF calcula `align: 'right'/'center'` con `getTextWidth()`, que NO incluye el espacio extra de `charSpace` (~`charSpace × (chars-1)` mm adicionales). Y los precios por m² sí requieren centavos.
- **Fix:** Helpers `spacedTextRight`/`spacedTextCenter` en `src/components/pdf/ficha/draw.ts` que suman el charSpace al ancho y alinean a mano; `fmtMoneyDec` en `theme.ts` para montos por m². Verificado renderizando el PDF y revisándolo visualmente.
- **Regla:** En jsPDF, NUNCA combinar `charSpace` con `align: 'right'` o `'center'` — usar los helpers de `ficha/draw.ts`. Otros glifos que NO existen en las fuentes estándar (WinAnsi) de jsPDF: ✓ y ● — dibujarlos con líneas/círculos (ver `drawCheck`/`drawBullet`). Los PDFs se pueden verificar visualmente compilando el módulo con tsc a CommonJS y corriéndolo en Node (jsPDF funciona en Node con imágenes como dataURL).

## 10. Sistema de marca BRIKA para PDFs (plantilla ficha técnica)
- **Error:** (Contexto, no bug) El PDF de propiedades usaba un diseño genérico con colores que no eran de la marca; la sección "Identidad Visual" de CLAUDE.md (navy `#1B2A4A` / naranja `#E8872A`) NO corresponde a la marca real de BRIKA.
- **Causa:** CLAUDE.md heredó la identidad del fork; la marca real de BRIKA es: tinta `#1A1A1A`, morado `#A41BDD` (oscuro `#7A14A8`, claro `#E4B8FF`), grises `#6B6B6B`/`#9A9A9A`, divisores `#E4E1E8`, fondo suave `#FAFAFB`. Tagline: "Consultoría en Inversiones Inmobiliarias Industriales". Contacto: contacto@brika.com.mx | 443 207 5122.
- **Fix:** `/dashboard/pdf` genera la Ficha Técnica Comercial BRIKA (2 págs. carta por propiedad) replicando `public/pdf/pdf-ficha-tecnica.pdf` en `src/components/pdf/ficha/` (theme/assets/data/draw/page1/page2/index). El logo es `public/images/brika-logo-fondo-negro.png` (cuadro negro 2500×2501, fondo `#121212`, wordmark en bbox x 310–2170 / y 915–1410); `cropLogoChip` lo recorta a "chip" y los bloques oscuros usan `#121212` para fundirse con él. Carta propuesta (PDF y DOCX) usa el mismo chip.
- **Regla:** CONFIDENCIALIDAD en fichas: nunca imprimir `parque`, `ubicacion` (dirección exacta), `unidad`/lote ni `notas_tecnicas` — solo municipio/estado y zona/corredor (ver encabezado de `ficha/data.ts`). Para colores de marca en PDFs usar `ficha/theme.ts`, no la sección de CLAUDE.md.

## 11. Hydration error en /dashboard/pdf + imágenes de ficha siempre vacías
- **Error:** "Hydration failed because the initial UI does not match what was rendered on the server" al abrir el módulo de PDF con una selección guardada. Además la ficha nunca traía foto de portada.
- **Causa:** (1) `usePdfSelection` inicializaba el estado leyendo `sessionStorage` en el primer render — el SSR renderiza 0 seleccionadas y el cliente renderiza N → mismatch. (2) `/api/pdf/images` consultaba `desarrollos_view` (esquema Kibah, inexistente en la BD de brika) → mapa de imágenes siempre `{}`. Los links reales de `inventario_industrial.links_imagenes_carpetas_drive` son CARPETAS de Google Drive privadas (devuelven 401; además son `/drive/u/2/…`, atadas a la cuenta del usuario) — no se pueden renderizar sin OAuth de Drive.
- **Fix:** (1) El hook arranca vacío y se hidrata en `useEffect` (con flag `hydrated` para no pisar el storage con `[]` antes de cargar). (2) `/api/pdf/images` ahora lee `inventario_industrial` (`getInventarioImagesByParques` en `lib/dal/properties.ts`), usando el link solo si es URL directa a imagen (jpg/png/webp). (3) El builder permite adjuntar manualmente la foto de portada por propiedad (`coverMap` por id, dataURL en memoria, prioridad sobre la BD) — se incrusta al generar, sin tocar la BD.
- **Regla:** En hooks de cliente, NUNCA leer `localStorage`/`sessionStorage` en el inicializador de `useState` de componentes que se renderizan en SSR — siempre hidratar en `useEffect`. Los links de Drive del inventario son carpetas privadas: no asumir que se pueden mostrar; si se quiere portada automática, se necesita integración con la API de Drive (OAuth con scope drive.readonly) o una columna con URL directa de imagen.

## 12. Fuente real de imágenes de propiedades: propiedad_image_sets + bucket Imagenes
- **Error:** (Sigue de #11) La ficha no traía foto automática; el intento con `links_imagenes_carpetas_drive` no era viable (carpetas privadas de Drive).
- **Causa:** La fuente real está en Supabase: la tabla `propiedad_image_sets` (`propiedad_id` uuid → `image_set_id` int) liga cada propiedad con una carpeta del bucket **público** `Imagenes` (`{image_set_id}/1.jpg, 2.jpg, ...`). 293 filas (cobertura total del inventario). Se descubrió listando el esquema vía OpenAPI de PostgREST (`GET /rest/v1/` con service role) y `storage.listBuckets()`.
- **Fix:** `getInventarioImagesByIds` en `lib/dal/properties.ts`: ids → sets → primera imagen del set (prioriza archivos `portada|fachada|cover`, luego orden natural) → URL pública por el endpoint de **transformación** (`/render/image/public/...?width=1600&quality=80` — los originales pesan >10MB; el proyecto tiene transformaciones habilitadas y hasta convierte webp→jpeg). `/api/pdf/images` ahora recibe `{ ids }` (antes `{ nombres }`) y devuelve el mapa keyed por id de propiedad; PdfBuilder/PdfPreview/generateFichaPdf indexan por `p.id`. La portada manual del builder (#11) sigue teniendo prioridad.
- **Regla:** Las imágenes de propiedades brika viven en el bucket `Imagenes` vía `propiedad_image_sets` — NO en Drive ni en `desarrollos_view`. Para servir imágenes del bucket usar el endpoint de transformación con `width`, nunca el original. `Property.id` es uuid (string) aunque el type legacy diga number — como clave de objeto funciona, pero no asumir numérico.

## 13. Carta Propuesta: picker de propiedades vacío (leía propiedades_view)
- **Error:** En `/dashboard/carta-propuesta` el selector de propiedades no mostraba nada — no jalaba de la base de datos.
- **Causa:** `/api/carta-propuesta/properties` seguía consultando `propiedades_view` (vista del esquema Kibah, inexistente en el proyecto Supabase de brika). Misma clase de bug que #6: ruta del fork nunca migrada.
- **Fix:** La ruta ahora consulta `inventario_industrial` (columnas reales: parque, unidad, zona_corredor, ubicacion, operacion, precio_total_venta, renta_mensual) con el cliente admin tras verificar sesión, y remapea con `mapInventarioRow` a las claves legacy que espera el `PropertyPicker` (nombre_kibah, colonia, direccion, precio_unidad…). En el mismo cambio se eliminó el campo "Director de ventas" del formulario y de la carta (PDF y DOCX) a pedido del usuario.
- **Regla:** Al tocar cualquier módulo del fork, verificar primero de qué tabla/vista lee su API: si menciona `propiedades_view`, `base_kibah` o `desarrollos_view`, hay que migrarla a `inventario_industrial` vía `KEY_TO_COLUMN`/`mapInventarioRow` (ver #6). Buscar con grep esas vistas antes de dar un módulo por migrado.

## 14. Carga Masiva migrada al esquema brika (inventario_industrial) + modo de mapeo libre
- **Error:** El módulo de carga masiva seguía siendo 100% Kibah: template con columnas residenciales (recámaras, colonia CDMX), insert a `base_kibah`/`pagina_web_kibah` (inexistentes en la BD de brika) y tipo "desarrollos".
- **Causa:** Módulo del fork nunca migrado (ver regla de #13).
- **Fix:** Reescrito completo hacia `inventario_industrial`: `columns.ts` define las 33 columnas reales (parque/unidad/operacion obligatorias por el unique constraint) con alias para auto-mapeo; template regenerado con marca BRIKA; el paso 1 ofrece 3 caminos — descargar template, subir template llenado, o "Tengo un archivo CSV o Excel" que abre un paso de MAPEO (cada campo del inventario se asigna a una columna del archivo, pre-mapeado por coincidencia normalizada, un encabezado solo puede alimentar un campo). Soporta .csv (parser propio con comillas y delimitador , o ;) y celdas Excel richText/hyperlink/fórmula. check-duplicates e insert consultan `inventario_industrial`; el 23505 se traduce a 409 amigable; se eliminó el concepto de `lote_carga` (la tabla no tiene esa columna) y el tipo "desarrollos".
- **Regla:** Los tres campos clave de cualquier carga son parque + unidad + operacion (unique). El insert convierte numéricos con `Number()` sobre valores ya limpiados de `$ , espacios` en el cliente; fechas viajan como ISO YYYY-MM-DD.

## 15. Módulo Asesores: tabla `profiles` completa, alta de usuarios y permisos por rol
- **Error:** `/dashboard/admin/asesores` mostraba tarjetas con 3 campos, excluía a los admins (`.neq('role','admin')`) y no permitía editar nada salvo rol/estado. El modal de alta usaba `useState(() => fetch(...))` como si fuera `useEffect` (funcionaba por accidente). `PATCH /api/asesores/[id]` no validaba el body y escribía `role` sin comprobar que existiera; un usuario no-admin con `asesores.edit` podía asignar `admin` a otro (escalada de privilegios). Además, la BD tiene (001) `CHECK (role IN ('asesor','admin'))` en `profiles.role`, así que asignar un rol personalizado de la tabla `roles` (p. ej. `gerencia`) falla con 23514.
- **Causa:** Módulo heredado del fork pensado solo para "asesores"; el sistema de roles (006) se agregó después sin adaptar la tabla `profiles` ni las rutas.
- **Fix:** DAL nuevo `lib/dal/asesores.ts` (paginación por cursor de 50, merge con `auth.admin.listUsers` para último acceso/email confirmado, validación de rol contra `roles`); `lib/dal/users.ts` queda solo para el perfil propio. Rutas con Zod (`lib/validations/asesor.ts`) y guardas: no-admin no puede crear/editar/eliminar admins ni otorgar `admin`; nadie se desactiva ni se cambia el rol a sí mismo. UI: tabla con las 9 columnas de `profiles` + último acceso, búsqueda/filtros, detalle con edición de todos los campos (nombre, email —también en auth—, rol, avatar, tema), reset de contraseña, permisos del rol en solo lectura, y pestaña "Roles y permisos" (solo admin) que reutiliza `RoleManager`/`RoleEditor` sin volver a mostrar "Roles" en el menú. `UserRole` pasa a `'admin' | 'asesor' | (string & {})`. SQL `sql/008-profiles-roles.sql`: CHECK → FK `profiles.role → roles(name)`, trigger `updated_at` en `roles`, claves de permisos faltantes (`formularios.view`, `carta_propuesta.view`, `carga_masiva.view`).
- **Regla:** Los permisos son POR ROL (`roles.permissions`), nunca por usuario; `admin` hace bypass total en `getUserPermissions` (`_isAdmin`). Cualquier permiso nuevo se agrega en 4 lugares: `ALL_PERMISSIONS`/`PERMISSION_GROUPS` (`types/roles.ts`), `PERM_LABELS` (`RoleEditor`), `requirePermission` en la API, y `ADMIN_PATH_PERMISSIONS` en `middleware.ts` si protege una página `/dashboard/admin/*`; y se otorga a los roles existentes con un SQL `UPDATE roles SET permissions = permissions || '{...}'`. Al escribir `profiles.role` validar primero que el rol exista en `roles`. Ojo: `/api/properties` (POST/PUT/DELETE) y `/api/upload` siguen verificando `role === 'admin'` a mano, así que marcar `propiedades.create/edit/delete` en un rol no-admin muestra los botones pero la API responde 403.

## 16. Carta Propuesta: campos condicionales y redacción industrial BRIKA
- **Error:** La carta seguía siendo la de Kibah: "compraventa del *departamento*", "calle X, *colonia* Y", "Valor del Departamento", fecha "Ciudad de México", firma "del desarrollador". Los 14 campos eran obligatorios (`canGenerate`) y PDF/Word imprimían todo aunque fuera 0 ("apartado de $0 MXN", "0 mensualidades de $0"). Además el PDF pesaba ~4 MB por carta: `addImage(..., 'PNG')` sin compresión incrusta los píxeles crudos del logo (el PNG pesa 25 KB). También había labels residenciales visibles en Propiedades ("Colonia"/"Alcaldía" en el detalle, placeholder del buscador) y `lib/validations/property.ts` (esquema Kibah) estaba muerto sin imports.
- **Causa:** Módulo del fork nunca adaptado al inventario industrial; la redacción estaba duplicada a mano en `generateCartaPdf.ts` y `generateCartaDocx.ts`.
- **Fix:** Redacción única en `components/carta-propuesta/cartaContent.ts` (`buildCartaContent`): cada frase se arma solo con lo que tiene valor (> 0 / no vacío) y se reacomoda (sin apartado no hay viñeta de apartado ni cláusula de "7 días hábiles"; enganche sin % omite el paréntesis; saldo con/sin mensualidades/escritura en 5 variantes; municipio/estado del inventario; "del lote industrial 7" vs "de la nave industrial 14"). PDF y Word solo dibujan ese contenido. Formulario partido (`CartaForm`, `CartaPreview`, `MoneyField`, `numeric.ts`, `cartaFormState.ts`) con solo propiedad/asesor/cliente obligatorios; campos: Dirección, Parque, Unidad, Desarrollador/propietario + 7 financieros (el % de pago restante se deriva como 100 − % enganche). El selector (`PropertyPicker`) y `/api/carta-propuesta/properties` usan columnas reales (`CartaProperty` en `types/carta-propuesta.ts`, id uuid string). `addImage(..., 'FAST')` → 70 KB por carta. Verificado en Node (tsc → CJS con `paths` en un tsconfig del scratchpad + shim de `assets.ts` que lee el logo de disco): 1 página, texto dentro de márgenes, sin palabras residuales, en 3 escenarios.
- **Regla:** `ubicacion` del inventario es un **link de Google Maps**, no una dirección: nunca prellenar "Dirección" con él (se muestra como link de apoyo). La redacción de cartas vive en UN solo archivo consumido por todos los formatos; nunca duplicar strings entre PDF y DOCX. En Windows no pueden coexistir `CartaForm.tsx` y `cartaForm.ts` (TS1149: rutas que solo difieren en mayúsculas) — usar nombres distintos. En jsPDF, siempre pasar `compression: 'FAST'` al incrustar PNG. `--paths` no se acepta en la CLI de tsc: para compilar a CJS un módulo con alias `@/` usar un `tsconfig` aparte con `baseUrl`/`paths` y resolver `@/` en Node con un hook de `Module._resolveFilename`.

## 17. Filtros de Propiedades: opciones duplicadas por mayúsculas/acentos ("Colon" y "Colón")
- **Error:** Los selects de filtro mostraban "Colon" y "Colón", "Lote industrial" y "Lote Industrial", "Nave industrial" y "Nave Industrial" como opciones distintas, y elegir una solo traía las filas escritas exactamente igual (`.eq`).
- **Causa:** `inventario_industrial` la alimenta Laravel/carga masiva sin normalizar; `/api/properties/filter-options` devolvía `DISTINCT` crudo y `/api/properties` filtraba con igualdad exacta. Además el DAL tenía `getProperties`/`getFilterOptions` muertos (las rutas consultaban inline).
- **Fix:** `lib/utils/normalize.ts` (`normalizeKey` = sin acentos + minúsculas + espacios colapsados; `groupVariants` agrupa y elige como etiqueta la variante más frecuente, desempate por acentos/mayúsculas) y `lib/dal/filter-options.ts` (`getFilterOptions` → una opción por texto; `resolveColumnVariants(columna, valor)` → todas las variantes crudas en BD, cache 60 s, invalidado en alta/edición/baja). Las rutas filtran con `.in(columna, variantes)`: sin extensión `unaccent` ni cambios de esquema. Verificado sobre los 443 registros: municipio 7→6 opciones, tipo_producto 5→3. Las opciones sirven también al `PropertyForm` y los filtros guardados con variantes viejas siguen funcionando (se resuelven por clave normalizada).
- **Regla:** Todo filtro de catálogo sobre texto libre del inventario debe pasar por `resolveColumnVariants` (nunca `.eq` directo) y sus opciones por `getFilterOptions`. Las etiquetas salen en formato Título (`toTitleLabel`: "Nave Industrial", "El Marqués"; conectores en minúscula "Apaseo el Grande"; siglas CDMX/QRO/PIQ/BTS intactas) a pedido del usuario (2026-08-20) — la etiqueta es solo visual, el filtro siempre usa las variantes crudas.

## 18. Ficha Técnica (PDF): el buscador "no servía" — paginado de 20 + "Cargar más"
- **Error:** En `/dashboard/pdf` el selector usaba `useProperties` (el mismo hook paginado de la tabla de Propiedades): 20 resultados por página ordenados por `created_at`, búsqueda en servidor con debounce y botón "Cargar más". Para armar una ficha con varias propiedades había que paginar a ciegas; la búsqueda se sentía rota (resultados parciales, orden por fecha).
- **Causa:** Componente heredado del fork pensado para 2,600 filas residenciales (regla #2: nunca más de 50 por request). El inventario de brika tiene 443 filas y el selector solo necesita 12 columnas para listar.
- **Fix:** `getInventarioList()` en `lib/dal/properties.ts` (versión ligera `InventarioListItem` en `types/inventario.ts`, bloques de 1000, orden parque + unidad numérica) servido por `GET /api/pdf/properties` (`pdf.view`) y reutilizado por `/api/carta-propuesta/properties`. `PdfPropertySelector` carga todo una vez y busca en el cliente sin acentos/mayúsculas y por varias palabras ("acupark 14"); al marcar una propiedad pide la ficha completa a `GET /api/properties/[id]` (nuevo handler GET, `propiedades.view|pdf.view|carta_propuesta.view`) porque la ficha necesita las ~35 columnas. `precioListado()` en `lib/utils/inventario.ts` unifica el precio mostrado (renta/mes vs venta) en ambos selectores.
- **Regla:** La regla #2 (≤ 50 por request) aplica a filas completas para tablas; para **selectores con búsqueda** se puede traer el inventario completo si es la versión ligera (≤ 12 columnas, hoy ~60 KB) — y pedir el registro completo solo al seleccionar. Si el inventario creciera a miles, virtualizar la lista antes que volver a paginar. Verificado que la búsqueda de servidor (`vocal → _` + `ilike`) sí funciona a nivel PostgREST: el problema era el flujo, no la consulta. Limitación: supabase-js solo entrecomilla valores de `.in()` con `, ( )`; si algún día aparecen variantes con espacios al inicio/final, validar que PostgREST las empareje (hoy no existen).

## 19. Galería de fotos en las tarjetas de /dashboard/propiedades (selección de imágenes)
- **Error:** (Contexto, no bug) Las tarjetas no mostraban ninguna foto aunque el inventario tiene imágenes en Storage (ver #12), y no había forma de ver ni elegir las fotos de una propiedad desde la app.
- **Causa:** El único consumidor de `propiedad_image_sets` era el módulo PDF (portada). Verificado en vivo (2026-08-21): 312 filas → solo ~47 sets distintos (muchas propiedades comparten carpeta), 0–11 fotos por set, nombres libres (`1.jpg`, `2 AP2.webp`, `Spartek 1.jpg`; hay archivos sin extensión que se ignoran), originales de 10–17 MB.
- **Fix:** DAL `lib/dal/property-images.ts` (sale de `properties.ts`, que rozaba las 300 líneas): listado por set cacheado 5 min en memoria, variantes `thumb` 320px / `cover` 640px / `full` 1600px por el endpoint `/render/image/`. Rutas `POST /api/properties/images` (`{ ids }` ≤ 50, Zod → portada + conteo) y `GET /api/properties/[id]/images` (todas las fotos), ambas con `requireAnyPermission(['propiedades.view','pdf.view'])`; `/api/pdf/images` conserva su contrato (solo importa del DAL nuevo). UI: `PropertyCardCover` (portada, "N fotos", contador de seleccionadas); click en la portada despliega `PropertyCardGallery` dentro de la tarjeta (tiles palomeables, Todas/Ninguna, abrir en grande); el click en el cuerpo sigue abriendo el detalle. La tarjeta dejó de ser un `<button>` (anidaba botones, HTML inválido): ahora es `<article>` con cuerpo `role="button"`. Hooks `usePropertyCovers` (cache por pestaña + lotes de 50, regla #2) y `useImageSelection` (propiedad → nombres de archivo, persistido en `localStorage` `brika-image-selection`, hidratado en `useEffect` por #11).
- **Regla:** La selección de imágenes todavía NO se guarda en BD: no existe tabla de visibilidad de imágenes (verificado vía OpenAPI de PostgREST). Cuando se defina el modelo, el único punto a cambiar es `loadFromStorage`/`saveToStorage` en `hooks/useImageSelection.ts`; la clave estable de cada foto es `image_set_id + nombre de archivo`, nunca el índice (el orden cambia al subir fotos) y, como varias propiedades comparten set, la visibilidad debe guardarse por `propiedad_id`, no por set. Para servir fotos del bucket siempre usar una variante con `width` (nunca el original) y listar por set con cache. → Superado por #20: la selección ya persiste en BD (`propiedad_imagenes_visibilidad`) y `useImageSelection` se eliminó.

## 20. Visibilidad de fotos para la página web: tabla `propiedad_imagenes_visibilidad`
- **Error:** (Sigue de #19) La selección de fotos vivía en `localStorage`; la página web (otro sistema, mismo proyecto Supabase) no podía leerla.
- **Causa:** Faltaba el modelo en BD. El usuario decidió (2026-08-21) que la web lea directo de Supabase, sin endpoint del CRM, y creó la tabla desde el dashboard.
- **Fix:** Tabla `public.propiedad_imagenes_visibilidad` (referencia en `sql/009-propiedad-imagenes-visibilidad.sql`): PK `(propiedad_id uuid FK inventario_industrial, image_name text)`, `visible bool default true`, `orden int` (sin uso), `updated_at`, `updated_by`. Regla **sin fila = visible**. DAL `lib/dal/image-visibility.ts` (`getHiddenNamesByPropertyIds` solo lee `visible = false`; `setImagesVisibility` hace upsert por PK mandando `updated_at` explícito porque el default no aplica en el conflicto). `getPropertyImages` devuelve `visible` por foto y la portada (`getInventarioCoversByIds`, también la que usa el PDF) pasa a ser la **primera foto visible** con `hidden` = ocultas. `PATCH /api/properties/[id]/images` `{ names, visible }` con `propiedades.edit`; valida que los nombres existan en el set (listado cacheado) y responde con el set fresco. Cliente: `hooks/usePropertyImages.ts` (store con `useSyncExternalStore`, toggle optimista con reversión por nombre si el servidor falla, `refreshCovers` al guardar); `useImageSelection` eliminado. UI: palomita = visible en la web (todas arrancan palomeadas), oculta = gris + etiqueta "Oculta"; "Mostrar todas / Ocultar todas"; pill ámbar "N ocultas" en la portada; solo lectura sin `propiedades.edit`. Verificado con el DAL real: ocultar `1 AP2.webp` mueve la portada a `2 AP2.webp` y `hidden` = 1; al mostrarla vuelve; filas de prueba eliminadas.
- **Regla:** Contrato con la web por propiedad: listar la carpeta del set como hoy y excluir `select image_name from propiedad_imagenes_visibilidad where propiedad_id = ? and visible = false`. Nunca interpretar "sin fila" como oculta. Escrituras solo por service role desde el CRM; si la web lee con la anon key necesita una policy de SELECT (RLS activado). Para usar `orden` u otro atributo por foto, tocar 3 lugares: DAL `image-visibility.ts`, mapper en `property-images.ts` y la galería.

## 21. Fotos cruzadas entre propiedades: el bucket no coincidía con las carpetas de Drive
- **Error:** Eizen 57 (y otras) mostraban fotos de otro parque en la tarjeta, y el link de Drive "llevaba a otras". El usuario sospechó del código o de `propiedad_imagenes_visibilidad`.
- **Causa:** Ni el código ni la tabla nueva. `propiedad_image_sets` es un espejo exacto de `inventario_industrial.links_imagenes_carpetas_drive` (47 carpetas de Drive distintas ↔ 47 sets, sin cruces; un parque con un link por unidad tiene un set por unidad; dos parques con el mismo link comparten set, y eso es intencional: "el link manda"). Lo que estaba mal era el **contenido** del bucket: la importación original subió las fotos de la carpeta "AEROPARK 2" al set 3 ("216", Eizen) y las de "216" al set 4 (Aeropark II), dejó vacío el set 14 ("57 y 58": sus fotos estaban en una subcarpeta), omitió `4.webp` en el set 47 y dejó 2 fotos en el set 25 que ya no existen en Drive. Además el set 9 tenía 5 `.HEIC` (no se muestran en navegador) y `216/3.png` pesaba 26.6 MB (>25 MB: el endpoint `/render/image/` responde 400 "source image too large").
- **Fix:** Con las carpetas de Drive compartidas como "Cualquier persona con el enlace → Lector" se pueden listar y bajar sin OAuth: listado vía `https://drive.google.com/embeddedfolderview?id=<folder>#list` (HTML con `flip-entry`; recursivo en subcarpetas) y descarga vía `https://drive.usercontent.google.com/download?id=<file>&export=download&confirm=t` (el `content-disposition` trae el nombre real con extensión, en bytes UTF-8 que Node lee como latin1 → decodificar). Herramientas en el scratchpad de la sesión (`drive-list.cjs`, `drive-all.cjs`, `drive-compare.cjs`, `drive-import.cjs`, `convert-images.ps1`): dry-run que compara por **tamaño exacto en bytes** (los eTag del bucket son multiparte `md5-N`, no comparables con el md5 del archivo; con mismo tamaño se conserva el nombre que ya tenía el bucket para no renombrar "1 (1).png" ni los espacios finos de macOS), respaldo a `_backup/<ts>/<set>/` con `storage.copy`, `upload(..., { upsert: true })` y `remove`. Conversión sin dependencias: PNG >25 MB → JPG máx. 3000 px q90 con `System.Drawing`; HEIC → JPG con WIC (`System.Windows.Media.Imaging.BitmapDecoder`, requiere la extensión HEIF de Windows, orientación correcta). Resultado (2026-08-21): 40 sets ya eran idénticos, 6 corregidos (3, 4, 9, 14, 25, 47); verificación posterior 46/46 iguales y todas las fotos nuevas responden 200 en `/render/image/`. El set 5 (Spartek) sigue privado en Drive y no se pudo comparar. El paso `--apply` lo ejecutó el usuario (el clasificador de auto mode bloquea escrituras/borrados en Storage desde el agente). Las herramientas quedaron consolidadas y permanentes en **`scripts/fotos/`** (`sync-fotos.cjs` + `drive.cjs` + `convert-images.ps1`, README incluido): dry-run por default, `--apply`, `--only N,M|nuevo`, `--delete-backups`; detecta carpetas de Drive sin set, les asigna el siguiente número libre y mapea las propiedades en `propiedad_image_sets`. Verificado tras consolidar: 47 carpetas (Spartek ya compartida), 0 cambios. Cache de descargas en `scripts/fotos/.cache/` (gitignored).

## 22. Límite de "Storage Image Transformations" rebasado (203/100): fotos en tamaño web + miniaturas propias
- **Error:** El dashboard de Supabase mostró 203 imágenes transformadas en el periodo contra 100 incluidas en Pro (103 de exceso). Cada imagen distinta servida vía `/render/image/` cuenta una vez al mes sin importar cuántos tamaños se pidan; con 213 fotos en el bucket el CRM rebasaba el límite cada mes, y los originales pesaban 8–17 MB.
- **Causa:** #12 eligió el endpoint de transformación porque los originales eran demasiado pesados para servirse directo; el costo por imagen no se consideró.
- **Fix:** Dejar de transformar por completo. `scripts/fotos/sync-fotos.cjs` (v2, con `optimize-images.ps1`) sube cada foto en **tamaño web** (máx. 1600 px, JPG q80, ~150–400 KB; un original jpg/webp de ≤1600 px y ≤400 KB se conserva tal cual) a `<set>/<nombre>` y su **miniatura** (640 px, JPG q75, ~30–70 KB) a `_thumbs/<set>/<nombre>`; orientación EXIF aplicada; HEIC/WebP decodificados con WIC. Estado por set en `image_sets` (tabla creada por el usuario el 2026-08-21: id, drive_folder_id, status, progress, last_synced_at…) y manifiesto `_manifest/<set>.json` (archivo de Drive → nombre en el bucket) para sincronizar solo lo nuevo. El DAL `property-images.ts` usa `getPublicUrl` directo (`thumb` → `_thumbs/`, `full` → web) y los `<img>` de tarjeta/galería hacen fallback a la versión web si falta la miniatura. Aplicado el 2026-08-21 (el usuario corrió `--apply --reoptimize`; se detuvo en el set 15 por un nombre con U+202F —espacio fino de capturas de macOS— que Storage rechaza con "Invalid key": `sanitizeName` ahora normaliza a ASCII seguro y la corrida se reanudó con `--apply`, que reconoce por manifiesto lo ya hecho). Verificado: 47 sets consistentes (web = miniaturas = manifiesto, `image_sets.status = ok`), 213 fotos web = 31.3 MB (antes ~1 GB), miniaturas 8.3 MB, muestras HTTP 200 por `/object/public/`, dry-run posterior 0 cambios.
- **Regla:** NUNCA usar `/render/image/` (`?width=`) en este proyecto; toda foto nueva entra por `sync-fotos.cjs` (o la automatización que lo reemplace) que genera web + miniatura. Carpetas `_thumbs/`, `_manifest/`, `_backup/` en la raíz del bucket no son sets (prefijo `_`). Si alguien sube una foto a mano a un set, sin miniatura, la tarjeta la muestra vía fallback pero conviene correr `--only <set> --reoptimize`.

## 23. Importación automática de fotos (sin scripts): photo-sync en el CRM
- **Error:** (Contexto, no bug) El flujo de #21–#22 dependía de correr `scripts/fotos/sync-fotos.cjs` a mano cada vez que una propiedad nueva trae link de Drive; el usuario pidió que "se haga sola" al subir o actualizar propiedades.
- **Causa:** Las funciones serverless de Vercel tienen límite de tiempo (10–60 s) y no hay cola de trabajos; importar una carpeta completa (6 fotos de 15 MB) en una sola request no cabe.
- **Fix:** Diseño **resumible por pasos**: `lib/services/photo-sync/` (`drive.ts` = port de `drive.cjs` sin disco; `optimize.ts` con **`sharp`** —única dependencia nueva—: web ≤1600 px q80 + miniatura 640 px q75, `.rotate()` EXIF, conserva jpg/webp ya web; `bucket.ts` = manifiesto/subidas/limpieza; `sync.ts`). `syncStep(setId)` procesa **una foto por llamada** (descarga + optimiza + sube + manifiesto + `image_sets.progress`) y finaliza cuando no falta ninguna (borra sobrantes, mapea propiedades, `status ok`, invalida el cache de listado). `detectPending()` es solo BD (≈0.5 s): crea filas `image_sets` (pending) para carpetas de Drive nuevas y mapea propiedades con link sin set. Disparadores: (1) `property-service` create/update y `carga-masiva/insert` llaman `detectPending()` en segundo plano; (2) el cliente (`hooks/usePhotoSync.ts`, store con `useSyncExternalStore`) al abrir Propiedades pide `GET /api/fotos/pending?detect=1` y hace `POST /api/fotos/sync {setId}` en bucle hasta `done` (banner "Importando fotos… k/n", tarjeta "Importando fotos…", refresca portadas y galerías); (3) sin cron (el usuario no quiso nada que pudiera costar): al terminar la importación, el cliente llama `POST /api/fotos/check` (≤8 sets por llamada, ≤6 llamadas) que revisa los sets no revisados en 24 h (`progress.checkedAt`), marca `pending` los que cambiaron en Drive y reintenta errores de >24 h; si algo cambió, el cliente da otra vuelta de importación. Fotos que fallan (HEIC en Vercel, archivo corrupto) quedan en `progress.skipped` y el set termina en `error` con mensaje, sin bloquear el resto. Verificado en vivo contra la BD (harness tsc→CJS): detectPending 0 pendientes en 0.5 s; `syncStep` de un set al día finaliza en 1.3 s; importación real de una subcarpeta de 3 PNG en un set temporal: 3–4 s por foto (PNG 2 MB → JPG 80–140 KB + miniatura 18–28 KB), finalización 1.7 s, limpieza completa. `npm run build` en verde.
- **Regla:** Toda foto nueva entra por photo-sync: NUNCA subir a mano al bucket. No hay cron ni variables nuevas en Vercel: los cambios dentro de Drive se detectan cuando alguien abre Propiedades (máximo una revisión por carpeta cada 24 h). El plan debe permitir `maxDuration = 60` en `/api/fotos/sync` y `/api/fotos/check` (en Hobby una foto de 15 MB puede exceder 10 s; el paso se reintenta en la siguiente visita). HEIC no se convierte en el servidor (sharp sin libheif): subir JPG a Drive. `scripts/fotos/sync-fotos.cjs` queda como herramienta manual (reoptimizar, emergencias); ambos comparten formato de manifiesto y tabla `image_sets`.

## 24. Orden de fotos, portada elegible y galería en el detalle
- **Error:** (Contexto, no bug) Pedido del usuario: escoger qué fotos van a la web (ya existía, #20), **su orden**, que la #1 sea la portada en el CRM y en la web, y que al abrir el detalle de la propiedad también se vean las fotos.
- **Causa:** La columna `orden` de `propiedad_imagenes_visibilidad` existía sin uso; la portada era la primera del orden natural de nombres y el modal de detalle no mostraba fotos.
- **Fix:** DAL `image-visibility.ts`: `getImageMetaByPropertyIds` (visible + orden por foto), `orderNames` (primero las que tienen `orden` ascendente, luego el resto en orden natural) y `setImagesOrder` (upsert `orden = índice` sin tocar `visible`; las fotos que no vienen en la lista pierden su posición para que un orden parcial no deje posiciones viejas). `property-images.ts`: `getPropertyImages` devuelve las fotos en ese orden y la portada (`getInventarioCoversByIds`, también la del PDF) es la **#1 visible**. `PATCH /api/properties/[id]/images` acepta `{ names, visible }` o `{ order: string[] }` (Zod union, nombres validados contra el set). Cliente: `reorderImages`/`setCoverImage` optimistas en `usePropertyImages`, `hooks/useDragReorder.ts` (arrastrar y soltar HTML5, sin librerías; soltar antes/después según la dirección), tile compartido `PropertyImageTile` (número de orden, chip "Portada", palomita de visibilidad, estrella "Hacer portada", abrir en grande) usado por `PropertyCardGallery` (tarjeta) y el nuevo `PropertyDetailGallery` (visor grande + tira de miniaturas + botones Hacer portada / Ocultar-Mostrar en la web) insertado al inicio del cuerpo de `PropertyDetail`; solo lectura sin `propiedades.edit` (`usePermissions`). Verificado en vivo con el DAL real: orden completo → portada = #1; ocultar la #1 → portada pasa a la siguiente visible; orden parcial → listadas primero, resto con `orden = null` al final, `visible` intacto. `npm run build` en verde.
- **Regla:** Contrato con la web por propiedad: `select image_name, visible, orden from propiedad_imagenes_visibilidad where propiedad_id = ? order by orden nulls last, image_name` → excluir `visible = false`, acomodar por `orden` (null al final, orden natural) y tomar la primera visible como portada; "sin fila" = visible y sin posición. La portada NUNCA se guarda aparte: es siempre la #1 visible del orden. Al reordenar mandar la lista completa de nombres.

## 25. Botón "Crear PDF" con enlace directo a la ficha (`/dashboard/pdf?id=…`)
- **Error:** (Contexto, no bug) Pedido del usuario: un botón "Crear PDF" en la propiedad que lleve al módulo de fichas sabiendo cuál eligió.
- **Causa:** El módulo PDF solo recibía propiedades por la selección múltiple guardada en `sessionStorage` (`usePdfSelection`); no aceptaba parámetros en la URL.
- **Fix:** `hooks/usePdfDeepLink.ts`: lee `?id=<uuid>` (o `?ids=a,b`, máx. 50) con `useSearchParams`, pide cada propiedad completa a `GET /api/properties/[id]`, la entrega a `PdfBuilder` y limpia la URL con `router.replace('/dashboard/pdf')` (un refresh no la vuelve a aplicar). `PdfBuilder` **reemplaza** la selección con la(s) del enlace (limpia portadas manuales, toast "Propiedad cargada en la ficha"); la selección múltiple (palomita FilePlus + banner "Generar PDF") sigue igual y no usa el parámetro. `pdf/page.tsx` envuelve `PdfBuilder` en `<Suspense>` (requisito de `useSearchParams`). Botón "Crear PDF" en la tarjeta (fila de acciones) y en el encabezado del detalle, visible solo con permiso `pdf.view` (`propiedades-view` pasa `onCreatePdf` → `router.push('/dashboard/pdf?id=…')`). No se agregó a la tabla (vista lista): desde ahí se llega por el detalle.
- **Regla:** Para enlazar a la ficha desde cualquier módulo usar `/dashboard/pdf?id=<uuid>`; el id es el uuid de `inventario_industrial` (no `id_propiedad` legacy). `useSearchParams` en un client component exige Suspense en la página aunque sea dinámica.

## 26. Ficha PDF: la foto de portada salía recortada ("no se ve en buen formato")
- **Error:** En la ficha técnica la foto de fachada se veía mal: el marco del hero mide 215.9 × 88.3 mm (proporción 2.44:1) y `cropToCover` recortaba la foto al centro para llenarlo, perdiendo ~40 % de una foto 3:2 (cielo y suelo) y mucho más en fotos verticales.
- **Causa:** Diseño heredado de #10: recorte tipo `object-fit: cover` para evitar la deformación de `addImage`, sin considerar que el marco es una franja muy ancha.
- **Fix:** `fitToBox` en `ficha/assets.ts`: la foto va **completa y centrada** (`object-fit: contain`, sin recorte ni deformación) y las franjas sobrantes se rellenan con la misma foto en versión "cover" desenfocada y oscurecida (velo `rgba(18,18,18,0.45)`), para que el título blanco siga legible. El desenfoque se logra dibujando la foto a 32 px y reescalándola con suavizado (funciona en todos los navegadores; `ctx.filter` no en Safari viejo). `ficha/index.ts` usa `fitToBox` en vez de `cropToCover` (que se conserva por si se quiere volver al recorte: es un cambio de una línea). Verificado reproduciendo la composición con sharp para una foto horizontal y una vertical.
- **Regla:** En fichas, nunca recortar fotos de propiedades para llenar marcos fijos: mostrar completa con relleno desenfocado. Si cambian las medidas del hero, `fitToBox` se adapta sola (recibe la proporción).

## 27. Carta Propuesta: selector Compra / Renta con redacción y campos propios
- **Error:** (Contexto, no bug) La carta solo sabía redactar propuestas de compra; con propiedades en renta mostraba un aviso y pedía capturar "valor de la inversión" a mano.
- **Causa:** El módulo (#16) se adaptó del fork residencial solo para compraventa.
- **Fix:** `CartaOperacion = 'compra' | 'renta'` en `CartaFormState`/`CartaPropuestaData`. `OperacionToggle.tsx`: control segmentado con semántica de radio (`role="radiogroup"`, flechas), prellenado desde `operacion` del inventario (`operacionFromInventario`: "renta"/"arrend" → renta) y cambiable a mano (nota si difiere del inventario). Campos: compra conserva los 7 financieros; renta captura **precio de renta mensual, meses de depósito y años de contrato** (prellena `renta_mensual` del inventario). `cartaContent.ts` queda partido en `buildCompra`/`buildRenta` sobre la misma estructura `CartaContent`, así PDF y Word no cambiaron: renta → título "PROPUESTA DE RENTA", "interés en tomar en arrendamiento", "Los arrendatarios ofrecen…", línea "Renta mensual", viñetas (renta mensual; depósito en garantía de N meses con el monto calculado meses × renta; vigencia de N años), cláusula de contrato privado de arrendamiento, firmas "arrendatario" / "arrendador". Verificado en Node con 4 escenarios (renta completa, solo precio, 1 mes/1 año sin propietario, compra sin cambios); concordancias en singular/plural.
- **Regla:** Cualquier campo nuevo de la carta se agrega en 4 lugares: `CartaFormState` (types), `makeEmptyForm`/`buildCartaData` (cartaFormState), `CartaPropuestaData` + la variante correspondiente en `cartaContent.ts`, y el formulario. Nunca tocar `generateCartaPdf`/`generateCartaDocx` para cambiar textos. Un `npm run build` puede fallar con `PageNotFoundError: /_document` si `next dev` está corriendo al mismo tiempo (conflicto en `.next`): reintentar.

## 28. Carta Propuesta sobre la plantilla oficial (`public/pdf/BRIKA_Plantilla_Carta_Propuesta_Ejemplo.pdf`)
- **Error:** (Contexto, no bug) El usuario entregó la plantilla oficial de la carta (2 págs. carta, WeasyPrint) y pidió que la carta use exactamente sus campos, prellenados desde el inventario.
- **Causa:** La carta anterior (#16, #27) era una redacción libre de compraventa/arrendamiento con otra estructura (título centrado, viñetas, firmas comprador/propietario).
- **Fix:** Reescritura del módulo con la estructura de la plantilla: encabezado (logo + lema a la derecha + regla morada), fecha "Ciudad, Estado, a D de mes de AAAA" a la derecha, bloque de destinatario (nombre en negrita, cargo, empresa, "Presente."), "Asunto:" morado + texto en negrita ("Propuesta formal de renta/compra de nave industrial — Nave industrial 63, Eizen 57, El Marqués, Querétaro"), "Estimado(a) X:", dos párrafos justificados, "TÉRMINOS DE LA PROPUESTA" (título espaciado + regla morada) y tabla de dos columnas con divisores grises: Inmueble, Superficie, Operación propuesta, filas de renta (renta mensual [+ IVA] con $/m², plazo forzoso, depósito en garantía, incremento anual) o de venta (precio de compra con $/m², forma de pago, enganche % con monto), fecha estimada de inicio/cierre, condiciones especiales, vigencia; aviso en cursiva gris, dos párrafos de cierre, "Atentamente," + firma (nombre, cargo, BRIKA Inmobiliaria, contacto), "ACEPTACIÓN (OPCIONAL)" con dos líneas de firma, y pie centrado. Las filas sin valor o que no apliquen no se imprimen; las notas "(solo RENTA/VENTA)" de la plantilla son instrucciones y no se imprimen. Prellenado desde el inventario: operación, parque/unidad (→ empresa destinataria y descripción), municipio/estado, superficie (`superficieFromDetail`: `area_base_calculo` si se reconoce; lotes → terreno; naves → rentable > construcción > terreno), precio de venta / renta mensual, y `disponibilidad` como fecha estimada; la página pide la fila completa a `GET /api/properties/[id]` al seleccionar (`CartaPropertyDetail`). PDF (`generateCartaPdf.ts`: clase `Writer` con párrafos justificados, saltos de página y tabla con filas envueltas; márgenes 22 mm, Helvetica 10 pt, interlineado 1.6, colores de `ficha/theme`) y Word (`generateCartaDocx.ts`: tablas sin bordes para encabezado/términos/aceptación, bordes inferiores grises, párrafo con borde morado) replican el diseño; la redacción vive solo en `cartaContent.ts`. Formulario en 7 secciones (`CartaForm` + `CartaFormSections`: TextField/TextArea/NumberField con sufijo/CheckField); `MoneyField` eliminado. Verificado renderizando el PDF real en Node (jsPDF + shim del logo) y rasterizando con pymupdf: compra y renta, 2 páginas, fieles a la plantilla.
- **Regla:** La plantilla manda: cualquier cambio de texto o de filas se hace en `cartaContent.ts` y se refleja en PDF y Word sin tocarlos. Para ver el PDF sin navegador: compilar con tsc a CJS, resolver `@/components/pdf/ficha/assets` a un shim que lea el logo del disco, y rasterizar con `python -c "import pymupdf"` (instalado en esta máquina; `pdftotext` de Git Bash sirve para extraer texto de una plantilla). Fecha de expedición: ciudad/estado de BRIKA (default Querétaro), no la ubicación del inmueble.

## 29. Botón "Exportar propiedades": todo el inventario a Excel
- **Error:** (Contexto, no bug) Pedido del usuario: descargar su base de propiedades de Supabase desde Propiedades.
- **Causa:** No existía ninguna salida masiva del inventario; solo la carga masiva (entrada).
- **Fix:** `GET /api/properties/export` (solo admin, `requireAdmin`) → `lib/services/inventario-export.ts` arma el libro con `exceljs` (ya dependencia): hoja "Inventario" con **"ID" + las 33 columnas reales** en el mismo orden y con los mismos encabezados de `INVENTARIO_COLUMNS` (carga masiva), así el archivo se puede volver a subir por carga masiva; encabezado negro/blanco como la plantilla, fila congelada, autofiltro, formatos `#,##0` en montos/m² y `#,##0.00` en precios por m². `lib/dal/inventario-export.ts` trae todas las filas (`select *`, bloques de 1000) ordenadas por parque y unidad numérica. En `propiedades-view` el botón (solo `role = admin`) hace `fetch` → blob → descarga con el nombre del `Content-Disposition` (`BRIKA-Inventario-AAAA-MM-DD.xlsx`) y muestra toast con el conteo (`X-Row-Count`). Verificado en Node contra la base real: 443 filas, 34 columnas, 84 KB, 1.9 s; releído con exceljs.
- **Regla:** Cualquier columna nueva del inventario se agrega en `INVENTARIO_COLUMNS` (carga masiva) y con eso entra también al export. El export es de TODO el inventario (no respeta filtros de la vista); si se pide exportar "lo filtrado", reutilizar los mismos filtros de `GET /api/properties` en el servidor, nunca exportar desde el cliente con lo paginado.

## 30. Ficha PDF de 2+ propiedades: Chrome cancelaba la descarga con "Error: Permisos insuficientes"
- **Error:** Al generar la ficha técnica con dos o más propiedades, Chrome mostraba "Error: Permisos insuficientes" y no guardaba `BRIKA_Fichas_Tecnicas_<fecha>.pdf`; con una sola propiedad sí funcionaba.
- **Causa:** `PdfBuilder` hacía `URL.createObjectURL(blob)` → `a.click()` → `URL.revokeObjectURL(url)` **en la misma línea**. Chrome lee el blob de forma asíncrona: con un PDF chico alcanza a leerlo antes de la revocación; con uno más grande (más fotos) la URL ya no existe cuando empieza a leer y aborta la descarga con ese mensaje (no es un problema de permisos de la app ni de Supabase). `generateTemplate.ts` y `ReviewStep.tsx` (carga masiva) tenían el mismo patrón; la carta y el export esperaban solo 1 s.
- **Fix:** Helper único `lib/utils/download.ts` → `downloadBlob(blob, filename)`: ancla temporal en el DOM, `click()` y revocación **60 s después**. Lo usan PdfBuilder (fichas), carta propuesta (PDF y Word), export de inventario, plantilla de carga masiva y reporte de errores.
- **Regla:** Toda descarga de Blob pasa por `downloadBlob`; NUNCA revocar la URL del blob de inmediato (ni con 1 s) después del `click()`.
- **Regla:** Ante "fotos equivocadas", comparar primero el bucket contra la fuente (Drive) por set; no asumir que el mapa `propiedad_image_sets` está mal. Cualquier foto que se suba al bucket debe ser JPG/PNG/WebP de **menos de 25 MB** (idealmente <5 MB) y con extensión; HEIC nunca. Respaldos en `_backup/` dentro del bucket: borrarlos cuando el usuario confirme. Las carpetas con nombre `_*` en la raíz del bucket no son sets.

## 31. Módulo "Logs": historial de auditoría de todo el CRM (`audit_logs`)
- **Error:** (Contexto, no bug) Pedido del usuario (2026-08-25): un apartado "Logs" en el dashboard con TODO el historial del CRM — quién modificó, qué usuario, qué hizo, ediciones campo por campo (incluido el link de Drive, m2, precios), altas, bajas, y el detalle de cada carga masiva (qué se subió nuevo y qué se actualizó) — EXCEPTO la selección/orden de fotos para la web.
- **Causa:** No existía ningún registro de actividad; los únicos rastros eran columnas `created_by`/`updated_by` sueltas.
- **Fix:** Tabla `public.audit_logs` (`sql/010-audit-logs.sql`: actor snapshot nombre/email para sobrevivir bajas de usuario, action/entity/entity_id/entity_label, `changes` JSONB `{Etiqueta: {antes, despues}}`, `metadata`, índices por fecha/entidad/actor, RLS de lectura admin o `logs.view`). Servicio `lib/services/audit-service.ts` (`logAudit` NUNCA lanza; helpers `diffFields`/`snapshotFields`/`pickFields`/`inventarioLabel`; etiquetas de campos del inventario tomadas de `INVENTARIO_COLUMNS` de carga masiva) + DAL `lib/dal/audit.ts` (insert, página por cursor ≤50 con filtros entidad/acción/actor/búsqueda, actores para el filtro). Enganchado en TODAS las rutas de escritura: propiedades (create/update con diff contra la fila cruda vía `getInventarioRawById`, delete con snapshot), carga masiva (acción `carga_masiva` con `metadata` {inserted, updated, nuevas[], actualizadas[{propiedad, cambios}]} capado a 300 filas, también en el caso de fallo parcial; el route ahora trae las filas existentes completas para el diff), webhooks (create/update/delete/test — headers solo como conteo, nunca sus valores), API keys (create/revoke — nunca la llave), asesores (create/update/delete/reset_password — nunca contraseñas), roles (con diff de permisos etiquetado), notas, desarrollos (legacy) y columnas. UI: `/dashboard/admin/logs` (item "Logs" en sidebar, permiso `logs.view`, middleware `ADMIN_PATH_PERMISSIONS`) con filtros, tabla expandible (Antes → Después por campo, chips de carga masiva) y "Cargar más" (página de 20, regla #2). Verify build passes ✅.
- **Regla:** Toda ruta de escritura NUEVA debe llamar `logAudit()` de `lib/services/audit-service.ts` DESPUÉS de que la operación tenga éxito (con `await`; nunca dentro del try de la operación de negocio como condición). EXCLUIDOS a pedido del usuario: `PATCH /api/properties/[id]/images` (visibilidad/orden de fotos web) y todo `photo-sync` (`/api/fotos/*`); también quedaron fuera calendario, filtros guardados y tokens de Google (módulos personales/ocultos — agregar solo si el usuario lo pide). En `changes`, las claves son la ETIQUETA visible del campo (no la columna): para inventario usar `INVENTARIO_FIELD_LABELS`. Secretos jamás al log: ni contraseñas, ni llaves API, ni valores de headers de webhooks. La tabla la crea el usuario pegando `sql/010-audit-logs.sql` (regla #6); mientras no exista, `logAudit` solo avisa en consola y nada se rompe.

## 32. Botón "Actualizar fotos": revisión de Drive al momento (sin esperar 24 h)
- **Error:** (Contexto, no bug) El flujo automático de photo-sync (#23) revisa cada carpeta de Drive máximo una vez cada 24 h al abrir Propiedades; si alguien subía fotos nuevas y quería verlas de inmediato, no había forma de forzar la revisión.
- **Causa:** Diseño sin cron (decisión del usuario para evitar costos): los cambios dentro de Drive solo se detectaban con `checkStaleSets` y su ventana de 24 h.
- **Fix:** `forceCheckProperty(propertyId)` en `lib/services/photo-sync/sync.ts`: resuelve la carpeta del link de la propiedad, corre `detectPending()` (crea el set si la carpeta es nueva), y según el estado del set — pendiente/importando → nada que hacer; en `error` → lo reencola de inmediato (el flujo automático espera 24 h); `ok` → compara Drive contra manifiesto SIN ventana y lo marca `pending` si cambió. Devuelve `ForceCheckResult` (`pending | unchanged | no_link | not_public`, en `types/inventario.ts`). Ruta `POST /api/fotos/refresh {propertyId}` (misma permisología que el resto de `/api/fotos/*`, `maxDuration 60`). Cliente: `refreshPropertyPhotos()` en `hooks/usePhotoSync.ts` — si quedó pendiente arranca `startPhotoSync(false)` (banner y portada "Importando fotos…" ya existentes). UI: `PropertyPhotoRefreshButton` (`components/properties/PhotoRefreshButton.tsx`, icono RefreshCw con spin) en la fila de acciones de la tarjeta (solo icono) y en el encabezado del detalle (con texto); solo se muestra si la propiedad tiene `link_drive`; feedback inline "Sin fotos nuevas" / "Error" (tooltip con el motivo, p. ej. carpeta no pública). Verify build passes ✅.
- **Regla:** El botón NO importa nada por sí mismo: solo marca el set `pending` y deja que el flujo normal (usePhotoSync → `/api/fotos/sync`) importe — nunca duplicar ese camino. Los renombres de fotos existentes en Drive NO se re-importan a propósito: la visibilidad/orden de la web va por nombre de archivo (#20) y renombrar en el bucket la rompería. Photo-sync sigue EXCLUIDO del audit log (#31) → matizado por #33: el BOTÓN sí se registra; el flujo automático no.

## 33. El botón "Actualizar fotos" sí se registra en el historial (Logs)
- **Error:** (Contexto, no bug) Pedido del usuario (2026-08-25): que presionar "Actualizar fotos" (#32) quede en el módulo Logs, aunque photo-sync en general esté excluido del historial (#31).
- **Causa:** La exclusión de fotos del audit aplicaba a todo `/api/fotos/*`; el usuario quiere rastrear específicamente el uso del botón manual.
- **Fix:** `POST /api/fotos/refresh` ahora llama `logAudit` con acción nueva **`actualizar_fotos`** (registrada en `AUDIT_ACTIONS` de `types/audit.ts` y en `AUDIT_ACTION_LABELS`/`AUDIT_ACTION_COLORS` — "Actualizó fotos", badge cyan): entidad `propiedad`, etiqueta "Parque · Unidad · Operación" (`getInventarioRawById` + `inventarioLabel`) y `metadata.resultado` en palabras ("Se encontraron cambios en Drive: importación iniciada" / "Sin fotos nuevas en Drive" / "carpeta no pública" / "sin link", con `mensaje` si hubo detalle). `LogChanges` ganó un renderizador de metadata genérica (`MetadataList`, etiquetas en `META_LABELS`) — antes un log sin `changes` que no fuera carga masiva mostraba "Sin detalle"; ahora también se ve el resultado de pruebas de webhook y revocaciones de API keys. La columna `action` de `audit_logs` es TEXT sin constraint: la acción nueva NO requiere tocar la BD (el cambio en `sql/010` es solo el comentario). Verify build passes ✅.
- **Regla:** Sigue excluido del historial el flujo AUTOMÁTICO de photo-sync (`/api/fotos/pending|sync|check`) y la selección/orden de fotos para la web; solo el botón manual (`/api/fotos/refresh`) se registra. Una acción nueva de audit se agrega en 3 lugares: `AUDIT_ACTIONS` (`types/audit.ts`), `AUDIT_ACTION_LABELS` y `AUDIT_ACTION_COLORS` (`constants.ts`) — sin SQL. Claves nuevas de metadata legibles: agregarlas a `META_LABELS` en `LogChanges.tsx`.

## 34. Logs técnicos: pestaña "Técnicos" con requests de la API (`technical_logs`)
- **Error:** (Contexto, no bug) Pedido del usuario (2026-08-25): registrar requests de la API — método, ruta, status HTTP, duración, errores — como una segunda parte del módulo Logs. Los logs de Vercel existen pero se esfuman en ~1 hora (Hobby).
- **Causa:** No había ningún registro propio de requests/errores; los `console.error` de las rutas solo viven en Vercel.
- **Fix:** Tabla `public.technical_logs` (`sql/011-technical-logs.sql`: method/route/path/status/duration_ms/user_id+user_email de la sesión/error, índices por fecha/status/ruta, RLS admin o `logs.view`, y purga automática de >90 días con **pg_cron** en un DO block que solo avisa si la extensión no está habilitada). `withTechLog(route, handler, {level})` en `lib/services/tech-log.ts`: envuelve el handler, mide duración, extrae el `error` del body (`res.clone().json()`) en ≥400, captura excepciones no controladas (registra 500 y relanza) y toma el usuario con `auth.getSession()` (lee cookie, sin viaje a la red — suficiente para etiquetar). Política de volumen: SIEMPRE errores ≥400; escrituras (POST/PUT/PATCH/DELETE); GETs solo si tardan > `SLOW_REQUEST_MS` (2 s); rutas de altísima frecuencia o POST-de-lectura (`fotos/sync|check|pending`, `properties/images`, `pdf/images`, `roles/my-permissions`, `carga-masiva/check-duplicates`) con `{ level: 'errors' }`. NUNCA se registran cuerpos de request (contraseñas/llaves). Aplicado a las **52 rutas** con un codemod (patrón uniforme `export async function METHOD` → `async function _METHOD` + `export const METHOD = withTechLog(...)`). Guard `NEXT_PHASE === 'phase-production-build'` en `recordTechLog`: durante `next build` se ejecutan GETs sin sesión (401) que meterían ~19 filas de ruido por deploy. DAL `lib/dal/technical-logs.ts` (insert + página por cursor con filtros status errors/4xx/5xx/slow, método y búsqueda en ruta/path/error/email); `GET /api/logs/tech` (`logs.view`). UI: el módulo Logs ahora tiene pestañas **Actividad | Técnicos** (`ActivityLogsTab`/`TechLogsTab`); la de Técnicos con badges de método y status, duración, usuario y fila expandible con el request real y el error completo. Verify build passes ✅.
- **Regla:** Toda API route NUEVA debe exportar sus handlers envueltos: `export const POST = withTechLog('/api/ruta', _POST)` (con `{ level: 'errors' }` si es de alta frecuencia o un POST de solo lectura). Un fallo del log técnico jamás afecta la respuesta (try/catch total + `console.error`). El log técnico NO sustituye al de Actividad: uno es "qué request pasó", el otro "qué datos cambiaron". Si un GET legítimo empieza a aparecer mucho en "Lentos", es un cuello de botella a optimizar, no ruido. La tabla la crea el usuario pegando `sql/011-technical-logs.sql`; mientras no exista, el CRM funciona igual (aviso en consola).

## 35. Ficha PDF: la selección se limpia sola tras "Generar y Descargar Ficha"
- **Error:** (Contexto, no bug) Pedido del usuario (2026-08-25): en `/dashboard/pdf`, el "limpiar" debe ocurrir DESPUÉS de darle a "Generar y Descargar Ficha" — la selección quedaba viva (sessionStorage de `usePdfSelection`) y la siguiente ficha arrancaba con las propiedades anteriores.
- **Causa:** `handleGenerate` en `PdfBuilder.tsx` solo generaba y descargaba; la limpieza era manual ("Limpiar selección" / "Nueva Ficha").
- **Fix:** `handleGenerate` llama `doReset()` (limpiar selección + portadas manuales) justo después de `downloadBlob` + toast de éxito. Solo en éxito: si la generación falla, la selección se conserva para reintentar sin volver a armar la ficha. `doReset` se movió arriba de `handleGenerate` (orden de declaración de los useCallback). Sin riesgo para la descarga: el blob ya está generado y `downloadBlob` mantiene viva la URL 60 s (#30). Los botones manuales "Limpiar selección" y "Nueva Ficha" siguen igual. Verify build passes ✅.
- **Regla:** Cualquier flujo de "generar y descargar" que consuma una selección debe limpiarla solo en el camino de éxito, nunca en el `finally` (un error no debe costarle al usuario rearmar su selección).

## 36. Ficha PDF: texto encimado en la franja de estadísticas (+ PDF de 4 MB)
- **Error:** En fichas de PractiPark (naves 71/80/81) la página 1 salía con la info encimada e ilegible: la franja de estadísticas mezclaba "Mínima 6 m, hasta 7 m en la parte de atrás", "Cortina metálica enrollable manual 3.70 m ancho x 4.30 m alto" y "3 KVA (trifásico)" unos sobre otros y el precio se salía del margen. Además cada ficha pesaba ~1.4 MB (4.1 MB por 3 propiedades).
- **Causa:** (1) `page1.ts` dibujaba el VALOR de cada stat a 16pt sin límite de ancho (`doc.text(s.value, …)`) en columnas de ~37 mm, y `data.ts` metía como stat el texto libre completo de `altura_libre`/`rampas`/`kva_disponibles` — diseñados para valores cortos ("12 m", "4 andenes"). Los datos de BD son válidos, solo verbosos. (2) El logo PNG se incrustaba SIN `compression: 'FAST'` (regla #16 ignorada en este módulo) y sin alias: 4 copias crudas del PNG de 1909×542 por propiedad.
- **Fix:** Dos capas. **Datos** (`data.ts`): `compactStat()` — valor corto pasa igual; largo se reduce a su rango numérico ("6 a 7 m", "3 KVA"); sin números, la stat se omite (LOGÍSTICA solo si andenes/rampas es un conteo puro). El texto completo sigue en las secciones de la página 2. **Defensa en el dibujo**: `fitTextSize()` en `draw.ts` (reduce la fuente hasta un mínimo y recorta con "…" si aún no cabe) aplicado a los valores de stats y al subtítulo (page1) y a las celdas del bloque comercial (page2); `drawChips` recorta chips más anchos que la fila; las filas de sección pasan de 2 a 3 líneas (ya no se comen "alto"/"pulido") y los círculos de ubicación tienen tope para no invadir el pie. **Peso**: `addImage(logo, 'PNG', …, 'brika-logo', 'FAST')` en los 4 puntos (alias = una sola copia del logo en el PDF): 4.1 MB → **120 KB**. Verificado con el arnés de #28 (tsc→CJS + shim de assets + pymupdf) sobre las 3 propiedades reales, antes y después. `npm run build` en verde.
- **Regla:** En PDFs, NINGÚN texto de origen libre (columnas de texto del inventario) se dibuja en un slot de ancho fijo sin pasar por `fitTextSize`/`splitTextToSize` — los datos de la BD pueden ser párrafos. Las stats de la franja solo aceptan valores compactos (`compactStat`); el detalle completo vive en la página 2. Todo `addImage` de un asset repetido lleva alias + `'FAST'` (una sola incrustación).

## 37. Propiedades repetidas en /dashboard/propiedades al dar "Cargar más" (paginación incoherente)
- **Error:** La lista de propiedades mostraba filas repetidas (Acupark 2 unidades 3, 66, 34…) tras "Cargar más"; el usuario sospechó duplicados en la BD. Verificado en vivo: la BD está limpia (443 filas, 0 combinaciones parque+unidad+operacion repetidas) — era solo la vista.
- **Causa:** `GET /api/properties` ordenaba por `created_at` (o la columna de sort) pero el cursor cortaba la siguiente página por `id` (`id < último-uuid`), un criterio SIN relación con el orden mostrado: la página 2 traía "filas con uuid menor", que incluían filas ya vistas (duplicadas) y excluían otras para siempre (faltantes). Lo agrava que el inventario solo tiene 3 valores distintos de `created_at` (cargas masivas: 191+150+102 filas con timestamp idéntico), así que el orden dentro de cada grupo era indefinido y cambiaba entre requests. El `getProperties` del DAL (código muerto, nadie lo llamaba) tenía la variante opuesta: cursor por `created_at` con `.lt`, que con esos empates masivos se habría saltado el resto del grupo.
- **Fix:** Paginación keyset con cursor compuesto en `lib/dal/keyset-pagination.ts` (`encodeKeysetCursor`/`decodeKeysetCursor` — base64url de `{v, id}` —, `cursorValue`, `applyKeysetCursor`). La ruta ordena `(columna_de_sort, id)` en la misma dirección con `nullsFirst: false` (nulos SIEMPRE al final) y corta con `or(col.gt."v", and(col.eq."v", id.gt.último), col.is.null)` (invertido para desc); si la última fila cayó en el bloque de nulos, el corte es `col.is.null AND id.gt.último`. Los valores van entrecomillados/escapados (comas/paréntesis en texto libre romperían el `or=`). El cursor se toma de la fila CRUDA (el filtro corre sobre columnas reales, no las mapeadas); un cursor con formato viejo se ignora y sirve la primera página. `getProperties`/`PropertyFilters`/`PaginatedResult` muertos eliminados del DAL. Verificado contra la BD real paginando TODO el inventario de 20 en 20 en 6 escenarios (created_at asc/desc, precio_total_venta asc/desc con 163 nulos, unidad asc, parque desc): 443/443 filas, 0 duplicadas, 0 faltantes; los únicos pares "fuera de orden" del arnés fueron collation de Postgres vs el comparador JS de prueba, dentro de una misma página. `npm run build` en verde.
- **Regla:** TODO cursor de paginación debe ser coherente con el ORDER BY: ordenar por `(columna, id)` y cortar por ese mismo par (usar `lib/dal/keyset-pagination.ts`), nunca ordenar por una columna y cortar por otra. Con datos de carga masiva asumir SIEMPRE empates masivos en `created_at` — un orden sin desempate es indefinido entre requests. Al armar filtros `or=` de PostgREST con valores de texto libre, entrecomillar y escapar el valor.
